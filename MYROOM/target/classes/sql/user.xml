<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user">
	<select parameterType="hashmap" id="selectUser" resultType="hashmap"> 
		select count(*)
		from user where 카카오번호 = #{kkono}
	</select>
	<insert parameterType="hashmap" id="addUser"> 
	insert into user (카카오번호, 이름, 이메일)
		values (#{kkono}, #{name}, #{email})
	</insert>
</mapper>