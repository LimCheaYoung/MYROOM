<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user">
	<insert parameterType="hashmap" id="addUser"> 
	insert into test (email, pw, name)
		values (#{email}, #{pw}, #{name})
		 </insert>
	<select parameterType="hashmap" id="login" resultType="hashmap"> select *
		from test where del_yn = 'N' and email = #{email} and pw = #{pw}
	</select>
	<update parameterType="hashmap" id="upUser"> 
	update test set email = #{email} ,pw = #{pw} ,name = #{name} where no = #{no} </update>
	<update parameterType="hashmap" id="out"> update test set del_yn = 'Y'
		where no = #{no} </update>
</mapper>